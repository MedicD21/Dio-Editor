version: "3.9"

networks:
  dio-network:
    driver: bridge

volumes:
  postgres_data:
  temp_media:

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: dio
      POSTGRES_PASSWORD: dio
      POSTGRES_DB: dio_editor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - dio-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dio -d dio_editor"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - dio-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - temp_media:/tmp/dio
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - dio-network
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - temp_media:/tmp/dio
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - dio-network
    command: celery -A tasks.render_task worker --loglevel=info --concurrency=2

  remotion:
    build:
      context: ./remotion-renderer
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - temp_media:/tmp/dio
    networks:
      - dio-network
    environment:
      - PORT=3001
      - TEMP_DIR=/tmp/dio

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    networks:
      - dio-network
    command: npm run dev
