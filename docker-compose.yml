version: "3.9"

networks:
  dio-network:
    driver: bridge

volumes:
  temp_media:

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - dio-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - temp_media:/tmp/dio
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - dio-network
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - temp_media:/tmp/dio
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - dio-network
    command: celery -A tasks.render_task worker --loglevel=info --concurrency=2

  remotion:
    build:
      context: ./remotion-renderer
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - temp_media:/tmp/dio
    networks:
      - dio-network
    environment:
      - PORT=3001
      - TEMP_DIR=/tmp/dio

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    networks:
      - dio-network
    command: npm run dev
